# agent-native-audit 完整方案文档：轻差异对齐策略（2026-04-15）

## 文档定位

本文基于 `agent-native-audit` 双侧唯一文件 `SKILL.md` 的真实 diff 写成。

---

## Part A：上游基线与代码事实

### A.1 核对范围

- source:
  - `plugins/compound-engineering/skills/agent-native-audit/SKILL.md`
- target:
  - `skills/agent-native-audit/SKILL.md`

### A.2 真实差异

当前只有一个真实差异：

- 上游引用：
  - `/compound-engineering:agent-native-architecture`
- 当前引用：
  - `/agent-native-architecture`

除此之外，没有发现额外文件，也没有其他目录级差异。

### A.3 现状判断

这是一个**纯命名/入口格式轻差异**，不是能力差距。

是否需要修复，取决于当前 skill 入口体系的统一规范：

- 如果 `spec-first` 当前统一采用不带前缀的本地 skill 引用，那么现状是正确的。
- 如果要统一为 canonical namespace，则应一起处理整仓相关 skill，不应单独修改本文件。

---

## Part B：当前产品边界下的正式实施方案

### B.1 目标

明确这是“命名契约问题”，不是功能缺口问题。

### B.2 建议方案

1. 当前不建议单独修复。
2. 把该差异明确标记为“纯入口命名迁移”。
3. 如后续全仓统一 skill 引用格式，再一起处理。

### B.3 可选保护

可新增轻量 contract test，验证：

- `agent-native-audit` 仍然引用 `agent-native-architecture`
- 引用格式与当前仓库命名规范一致

### B.4 最终建议

这是轻差异项，当前不应按“能力修复”推进，而应纳入全仓命名契约治理。
