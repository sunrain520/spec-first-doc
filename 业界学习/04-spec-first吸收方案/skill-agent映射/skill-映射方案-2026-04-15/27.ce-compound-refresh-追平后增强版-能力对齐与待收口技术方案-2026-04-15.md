# spec-compound-refresh 完整方案文档：`ce-compound-refresh` 完成态复核与本地分叉定版（2026-04-15）

## 文档定位

本文基于 `ce-compound-refresh` / `spec-compound-refresh` 的真实目录、support files、相关 consumer 与测试接线写成。

本文不复述旧 plan 的过时判断，而是只回答：

1. 当前 `spec-compound-refresh` 已经完成了什么
2. 当前与上游的剩余差异应如何定性
3. 在总映射表里应标成什么状态

结论先行：

- `spec-compound-refresh` 文件集合已齐
- `spec-compound-refresh` support files 已与本地 `spec-compound` contract 对齐
- 与上游的剩余 diff 主要是命名空间迁移与 intentional divergence，不再构成“未完成”
- 因此当前应标记为 `代码修复已完成`

---

## Part A：代码事实

### A.1 核对范围

- source:
  - `plugins/compound-engineering/skills/ce-compound-refresh/SKILL.md`
  - `plugins/compound-engineering/skills/ce-compound-refresh/assets/resolution-template.md`
  - `plugins/compound-engineering/skills/ce-compound-refresh/references/schema.yaml`
  - `plugins/compound-engineering/skills/ce-compound-refresh/references/yaml-schema.md`
- target:
  - `skills/spec-compound-refresh/SKILL.md`
  - `skills/spec-compound-refresh/assets/resolution-template.md`
  - `skills/spec-compound-refresh/references/schema.yaml`
  - `skills/spec-compound-refresh/references/yaml-schema.md`
- 邻接依赖：
  - `skills/spec-compound/SKILL.md`
  - `agents/research/learnings-researcher.md`
  - `tests/unit/spec-compound-contracts.test.js`
  - `tests/unit/docs-solutions-frontmatter.test.js`
  - `tests/smoke/cli.sh`

### A.2 文件集合状态

当前 `spec-compound-refresh` 文件集合与上游一致：

| 文件 | 上游 | 当前 |
|---|---|---|
| `SKILL.md` | 有 | 有 |
| `assets/resolution-template.md` | 有 | 有 |
| `references/schema.yaml` | 有 | 有 |
| `references/yaml-schema.md` | 有 | 有 |

其中：

- `assets/resolution-template.md` 已与上游字节级一致
- `references/schema.yaml` 已与本地 `spec-compound` 完全同构
- `SKILL.md` 与上游仍有 diff，但审查链路已定性为 intentional divergence
- `references/yaml-schema.md` 仅剩 quick reference 文案层精简差异

### A.3 已完成部分

当前已经完成的部分包括：

1. 文件集合补齐
2. 双模板 `resolution-template.md` 恢复
3. 双 track 相关 schema / template 已恢复并接入 refresh
4. `spec-compound` 主 workflow 已能生产符合双 track 合同的 `docs/solutions/`
5. `learnings-researcher` 与 `docs-solutions-frontmatter` 验证器已按双 track 模型工作
6. `spec-compound-contracts.test.js` 已把 refresh schema / template 纳入 contract coverage

因此：

- `spec-compound-refresh` 已不再是“本地单轨残缺版本”
- 它已经站在一个正确的数据与 consumer 基座上

### A.4 当前剩余差异的正确定性

#### A.4.1 `SKILL.md` 仍有 diff，但不再属于未收口缺陷

当前 `skills/spec-compound-refresh/SKILL.md` 与上游相比，仍不仅是命名空间替换，还包括多处行为与说明差异，例如：

1. `ce:brainstorm` → `spec:brainstorm` 这类命名迁移
2. 关于 subagent prompt 的说明被本地改写
3. 多处 discoverability / follow-up commit / instruction file edit 说明被本地重写
4. 若干“何时推荐 `compound` / `compound-refresh`”的文本被调整

但这类差异已经被审查链路明确定性为 `Intentional divergence`，不是遗漏同步：

1. discoverability check 已同步
2. refresh contract 已同步
3. 保留本地 `spec:*` 命名空间与表述，不再追求字节级一致

因此这里应视为**定版分叉**，不是“待收口”。

#### A.4.2 `references/schema.yaml` 不应再被视为未完成

如果只拿 `ce-compound-refresh` 对比，`skills/spec-compound-refresh/references/schema.yaml` 确实仍有以下差异：

1. knowledge track 中 `applies_when` 被设为 required
2. 上游关于 backward compatibility 的注释与规则未完整保留
3. validation rules 的表述与上游不完全一致

但对当前仓库来说，真正应比较的是它是否已经与**本地 source-of-truth contract** 对齐。当前代码事实是：

1. `skills/spec-compound-refresh/references/schema.yaml`
2. `skills/spec-compound/references/schema.yaml`

二者已经同构。

这意味着：

- refresh 使用的 support-file contract 已经接入本地双 track 基线
- `applies_when required` 是当前仓库的正式合同，不是 refresh 独有漂移
- 这部分不能再作为 `spec-compound-refresh` “部分完成”的证据

#### A.4.3 `references/yaml-schema.md` 仍有轻微说明文案差异

当前 `skills/spec-compound-refresh/references/yaml-schema.md` 相对上游仍有 quick reference 风格的压缩，例如：

1. 上游的 track 说明更完整
2. 上游的 required / optional / validation guidance 更细
3. 当前共享字段段落比 `spec-compound` 版本更简

但这类差异只影响说明密度，不影响运行时能力、consumer 接线或测试合同，因此应视为**低风险文案分叉**，而不是未完成功能。

### A.5 为什么当前应该标记为已完成

`spec-compound-refresh` 当前已满足“完成态”的三个条件：

1. source-of-truth 主 contract 已落地
2. 相关 consumer / validator 已按该 contract 工作
3. contract tests 已覆盖 refresh 的 schema / template 基座

当前剩余差异并不构成“功能未完成”，因为：

1. `SKILL.md` 差异已被审查文档定性为 intentional divergence
2. schema 差异已经被本地 `spec-compound` 基线吸收
3. `yaml-schema.md` 只剩说明文案层差异

因此继续标记为 `部分完成` 反而会误导读者，以为 refresh contract 还没落地。

---

## Part B：完成态判定

### B.1 判定依据

当前完成态判断基于以下代码事实：

1. `tests/unit/spec-compound-contracts.test.js` 已同时覆盖 `spec-compound` / `spec-compound-refresh` 的 schema 与 template
2. `tests/unit/docs-solutions-frontmatter.test.js` 已冻结 knowledge / bug 双 track frontmatter contract
3. 批次 D 审查报告已将 `skills/spec-compound-refresh/SKILL.md` 定性为“已同步 discoverability 与 refresh contract”
4. matrix deep audit 已将 `skills/spec-compound-refresh/SKILL.md` 定性为 `Intentional divergence`

### B.2 总映射表应如何回写

在总映射表里，`ce-compound-refresh -> spec-compound-refresh` 当前最准确的状态是：

- `方案分析状态 = 已完成（已归档）`
- `代码修复状态 = 已完成（refresh contract、双 track support files、consumer 与验证器已落地；剩余为 intentional divergence）`

### B.3 后续维护建议

后续如果还要继续优化，只建议做两类事情：

1. 统一 `spec-compound` / `spec-compound-refresh` 两份 `yaml-schema.md` 的说明密度，减少读者困惑
2. 若未来要重新追平上游文案，再单独做一次“文档说明同构化”整理

这两项都属于增强或清洁工作，不影响当前完成态。

---

## 最终结论

`spec-compound-refresh` 已完成主能力、双 track support files、consumer 接线与验证器保护。

当前与上游剩余的 diff，已可分为两类：

1. 命名空间迁移与本地表述分叉
2. 说明文档层的轻量文案差异

它们都不再构成“代码修复未完成”。

因此当前应明确标记为：

- `方案分析状态：已完成（已归档）`
- `代码修复状态：已完成（refresh contract、双 track support files、consumer 与验证器已落地；剩余为 intentional divergence）`
