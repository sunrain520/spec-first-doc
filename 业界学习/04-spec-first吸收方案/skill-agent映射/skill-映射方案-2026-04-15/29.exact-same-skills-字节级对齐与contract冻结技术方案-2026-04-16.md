# exact-same skills 字节级对齐与 contract 冻结技术方案

> Lifecycle: historical-input / external-reference. 本文保留历史 CRG/CE/ECC 方案、迁移或对比材料；其中 `src/crg`、`spec-first crg`、`graph.db`、`better-sqlite3`、`.claude-plugin`、`CRG Stage-0`、`ECC`、`compound-engineering-plugin`、命令数量和文件数量等旧口径可能已过期。当前 source of truth 以 `docs/archive-index.md`、`docs/README.md`、根目录 README、`docs/05-用户手册/`、`docs/contracts/`、`skills/`、`src/cli/`、`CHANGELOG.md`、`spec-mcp-setup` 和 `spec-graph-bootstrap` 为准。

日期：2026-04-16

## 范围

本次收口的 skill：

- `agent-native-architecture`
- `andrew-kane-gem-writer`
- `dspy-ruby`
- `gemini-imagegen`
- `git-clean-gone-branches`

目标不是重写这些 skill，而是以代码事实确认它们与上游 `compound-engineering-plugin` 当前源码保持一致，并把这一事实固化为可回归的 contract tests 与审计状态。

## 上游代码事实

本次以上游仓库快照 `plugins/compound-engineering/skills/` 为基准，对 5 个 skill 执行目录级只读核对：

- `agent-native-architecture`：`diff -qr` 无差异
- `andrew-kane-gem-writer`：`diff -qr` 无差异
- `dspy-ruby`：`diff -qr` 无差异
- `git-clean-gone-branches`：`diff -qr` 无差异
- `gemini-imagegen`：唯一差异为当前工作区存在未跟踪目录 `skills/gemini-imagegen/scripts/__pycache__/`，不属于源码能力差异

因此，这 5 个 skill 的“功能追平”在源码层已经成立。本轮真正需要补的是：

1. 把关键 contract 冻结成 unit tests，避免后续漂移
2. 将审计文档中的 `代码修复状态` 从 `未开始` 更新为完成态
3. 同步 `CHANGELOG.md` 与版本记录，形成治理闭环

## 当前仓库的最小充分动作

### 1. 新增 contract tests

新增以下测试文件：

- `tests/unit/agent-native-architecture-contracts.test.js`
- `tests/unit/andrew-kane-gem-writer-contracts.test.js`
- `tests/unit/dspy-ruby-contracts.test.js`
- `tests/unit/gemini-imagegen-contracts.test.js`
- `tests/unit/git-clean-gone-branches-contracts.test.js`

这些测试覆盖：

- skill identity/frontmatter
- 核心能力锚点
- `ClaudeAdapter` / `CodexAdapter` runtime transform
- 脚本、assets、references 的存在性与关键语义
- `git-clean-gone-branches` discovery-only safety contract

### 2. 不修改 skill 正文本体

由于上游源码对比已证实这 5 个 skill 在当前仓库中已对齐，本轮不对以下目录做功能性改写：

- `skills/agent-native-architecture/`
- `skills/andrew-kane-gem-writer/`
- `skills/dspy-ruby/`
- `skills/gemini-imagegen/`
- `skills/git-clean-gone-branches/`

这样可以避免“为了表示在做事而改动已正确代码”的无效 churn。

### 3. 审计口径

这 5 个 skill 的完成态定义为：

- 上游目录 diff 已核对
- 当前 skill 已在 `skills/` 中纳入源集合
- 新增 contract tests 已通过
- 审计文档 `代码修复状态` 已更新

## 每个 skill 的完成态说明

### `agent-native-architecture`

- 结论：与上游字节级一致
- 本轮收口：新增 architecture principles + intake/routing + runtime transform contract tests

### `andrew-kane-gem-writer`

- 结论：与上游字节级一致
- 本轮收口：新增 Kane 风格 entry point / Rails integration / anti-pattern guardrails contract tests

### `dspy-ruby`

- 结论：与上游字节级一致
- 本轮收口：新增 signatures/modules/providers/optimization，以及 assets/references contract tests

### `gemini-imagegen`

- 结论：源码与上游一致；仅有本地 `__pycache__/` 非源码差异
- 本轮收口：新增 generate/edit/compose/multi-turn chat、requirements 与脚本集合 contract tests

### `git-clean-gone-branches`

- 结论：与上游字节级一致
- 本轮收口：新增三步清理流程 + `scripts/clean-gone` discovery-only safety contract tests

## 验证命令

```bash
npx jest tests/unit/agent-native-architecture-contracts.test.js tests/unit/andrew-kane-gem-writer-contracts.test.js tests/unit/dspy-ruby-contracts.test.js tests/unit/gemini-imagegen-contracts.test.js tests/unit/git-clean-gone-branches-contracts.test.js --runInBand
```

```bash
git diff --check -- tests/unit/agent-native-architecture-contracts.test.js tests/unit/andrew-kane-gem-writer-contracts.test.js tests/unit/dspy-ruby-contracts.test.js tests/unit/gemini-imagegen-contracts.test.js tests/unit/git-clean-gone-branches-contracts.test.js CHANGELOG.md docs/08-版本更新/README.md "业界学习/04-spec-first吸收方案/能力映射/9.spec-first-vs-compound-engineering-plugin-全量同步审计-2026-04-14.md"
```

## 结论

这 5 个 skill 不需要再做“功能同步开发”，而是需要完成“证据化收口”。本轮通过上游目录核对 + contract tests + 审计状态回写，已把它们从“事实存在但文档仍标未开始”收口为“已验证完成”的维护状态。
